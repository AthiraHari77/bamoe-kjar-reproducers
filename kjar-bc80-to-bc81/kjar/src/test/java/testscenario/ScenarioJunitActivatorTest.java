package testscenario;

import org.junit.runner.RunWith;
import org.drools.scenariosimulation.backend.runner.ScenarioJunitActivator;

/**
 * JUnit activator that runs all .scesim files in this project.
 * Business Central generates this class automatically; we replicate it here
 * so the scenario tests run with: mvn test
 */
@RunWith(ScenarioJunitActivator.class)
public class ScenarioJunitActivatorTest {
}
